[package]
name = "image-match-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.24.5", optional = true}
num = { version = "0.4.0", optional = true }
[features]
img = ["image", "num"]